# 🚀 OntologySystem 全栈架构重构与功能优化蓝图 (Vibe Coding Master Plan)

## 👤 角色与上下文设定 (Context & Persona)

你是一位精通 **React**、**AntV G6 (图可视化引擎)**、**FastAPI**、**LLM 提示词工程**以及 **RDF/OWL 语义网标准**的资深全栈架构师。
当前项目是一个自动化的“知识图谱/本体构建系统”。现在的代码存在以下痛点：大模型提取混乱（类与实例混为一谈）、前端连线杂乱形成孤岛、编辑交互被 Drawer 弹窗严重遮挡视线、图谱缺乏 Neo4j 级别的拓扑美感。
**你的任务**：完整重构该系统的核心链路，将其打造为一个“人机协同”、“支持意图定向”、“体验媲美 Neo4j 图数据库”的专业图谱工程平台。

---

## 🎯 总体业务目标 (Core Objectives)

1. **构建阶段（OntologyBuilderPage）**：
  - 支持多文档上传（Word/PDF/PPT）并提取，或**直接上传 TTL 文件**进行可视化调整。
  - 彻底实行**“两步走”提取法**：先抽骨架（Schema），人工确认识别后再抽实例（Instance）。
  - 全过程支持**全量 CRUD**：新增/删除类和实例、配置起止节点和类型建立关系、修改属性。
  - 实现**实体对齐与防呆校验**：系统接管确定性 ID 生成，支持人工合并同义词节点，拦截非法关系飞线。
2. **资产展示阶段（AssetDetailPage）**：
  - 复用构建期的图谱渲染能力，但**完全关闭编辑功能**（只读模式）。
  - 保留图谱拖拽、缩放、点击查看属性、大纲树联动等浏览交互。
3. **全局共性能力**：
  - 使用 **AntV G6** 替换原有的 React Flow，实现**弹簧-电荷力导向布局**，并支持一键自动布局。
  - 图谱支持**“一键隐藏/显示实例”**全局开关。
  - 全过程的图数据（JSON）必须与后端的 Neo4j 数据库以及 `.ttl` 物理文件保持**双向同步持久化**，并支持前端导出 TTL。

---

## 🛠️ 模块一：后端抽取引擎与数据治理 (Backend Architecture)

### 1. 两阶段抽取 API (Two-Stage LLM Extraction)

- **API 1：带意图的骨架提取 (`POST /extract/schema`)**
  - **输入**：用户上传多个文档后的文档解析文本 + `user_intent` (用户填写的意图字符串，可选)。
  - **逻辑**：若有 `user_intent`，则在 Prompt 中强制模型聚焦该领域；否则进行通用概念提取。
  - **强约束**：**绝对禁止提取实例**。仅允许提取 `OWL Class` (类)、`Object Property` (关系逻辑定义) 和 `Data Property` (属性定义)。
- **API 2：强约束下的实例提取 (`POST /extract/instances`)**
  - **输入**：文档文本 + **完整 Schema Graph Data**（前端传来的、经过用户 CRUD 后的 JSON 数据）。
  - **逻辑**：将 Schema 转化为约束规则注入 Prompt。模型必须且只能实例化 Schema 中已定义的类。
  - **防御性拦截**：校验 LLM 返回的实例连线，若起点/终点不符合 Schema 中定义的 Domain/Range 约束，直接丢弃该边。

### 2. 数据一致性与治理机制 (Data Governance)

- **术语规范化**：API 1 的 Prompt 需强制要求剥离无意义助词（如将“负责的人”修正为“负责人”），合并同义词。
- **全局语义聚合 (Map-Reduce)**：在 API 1 长文档分块提取后，增加一个轻量级 LLM Reduce 步骤，对全量 Schema List 进行同义词融合去重。
- **确定性 ID 生成**：彻底废弃 LLM 随机生成的 ID。后端在聚合完成后，强制使用 `MD5(Label + Node_Type)` 算法生成系统的内部唯一 ID（如 `Class_5f3a...`）。
- **TTL 实时同步**：在保存草稿/发布接口中，利用 `rdflib` 将前端提交的最新的 JSON Nodes/Edges 结构反向序列化，覆盖生成后端的 `.ttl` 文件，确保随时下载的都是最新快照。

---

## 🎨 模块二：前端图引擎重构 (AntV G6 Integration)

**指令：全面移除 React Flow 依赖，使用 AntV G6 (v5或最新稳定版) 接管可视化。**

### 1. 视觉分层体系 (Neo4j Style Hierarchy)

- **节点 (Nodes)**：强制使用 `Circle` 形状，废弃矩形。
  - **类 (Class)**：大尺寸 (e.g., 50px-60px)，深色/冷色系背景（如蓝色）。
  - **实例 (Instance)**：小尺寸 (e.g., 30px-40px)，浅色/暖色系背景（如橙色）。
  - **文字标签**：在圆内居中，溢出显示省略号 (`...`)，悬浮显示完整 Tooltip。
- **连线 (Edges)**：
  - **类之间关系连线**：实线 + 箭头 + 深色，文字沿线展示。
  - **实例归属关系连线** (`rdf:type` / `instance_of`)：**保留箭头**（明确实例指向类），但样式必须配置为 **浅灰色 + 虚线 (`lineDash: [5, 5]`)**，极大弱化视觉干扰。

### 2. 智能布局与防抖控制 (Smart Layout)

- **力导向布局**：初始加载图谱时，配置 G6 的 `force` 布局，让实例自动环绕父类节点舒展。
- **坐标冻结 (极度关键)**：监听 G6 的 `afterlayout` 事件，布局结束后立即**锁定所有节点的 x/y 坐标**。确保用户后续手动拖拽节点或新增节点时，全图不会发生剧烈的物理重排抖动。

### 3. 画布内核心交互 (Canvas Behaviors)

- 启用内置交互：`drag-canvas`, `zoom-canvas`, `drag-node`, `click-select`。
- **连线交互**：配置 `create-edge` behavior，支持用户通过鼠标拖拽建立两个节点间的关系。
- **快捷键**：支持选中节点/边后按下 `Delete` 键触发删除操作，也可支持右键鼠标选择删除进行删除操作。
- **人工节点合并 (Merge Nodes)**：支持按住 `Shift` 键选中两个类节点，右键选择“合并节点”。将废弃节点的所有连线和属性转移到主节点上，然后调用 `graph.removeItem` 清除废弃节点，解决语义重复问题。

---

## 🧭 模块三：工作流与 UI/UX 重构 (Workflow & UI Navigation)

### 1. 四步状态机工作流 (Builder State Machine)

在 `OntologyBuilderPage` 中实现如下流转：

- **Step 1: 上传与配置** -> 支持上传文档或 `.ttl` 文件。若为文档，弹窗输入“提取重点意图”，随后调用 API 1。(若为 TTL，则解析后直接跳入全量渲染)。
- **Step 2: 骨架审查 (Schema Mode)** -> G6 仅渲染蓝色类节点。开放**全量编辑权**（右键/侧边栏新增类、配置起止节点新增关系）。
- **Step 3: 提取实例 (Instance Mode)** -> 点击“提取实例”，携带当前修改后的 Schema 数据调用 API 2，G6 增量渲染橙色实例节点。
- **Step 4: 保存与发布** -> 开放实例微调。点击顶部【保存草稿】触发后端 Neo4j 与 TTL 同步。

### 2. 左侧：全局大纲树 (Outline Tree)

- 用折叠式目录树替换现有左侧面板：结构为 `类 (🔵) -> 其包含的实例 (🟠)`。
- **数据统计**：顶部实时展示当前的“类数量”、“实例数量”、“关系数量”。
- **联动定位**：点击树中的节点，调用 G6 API (`graph.focusItem`) 使画布平滑飞行并聚焦放大至该节点。

### 3. 右侧：属性面板 (Fixed Property Panel)

- 使用现有代码中的 Drawer (抽屉弹窗) 组件。
- 选中类节点、实例节点或者边，右侧则弹出属性面板可进行修改删除，与当前代码逻辑类似。

### 4. 资产中心展示页 (`AssetDetailPage`)

- 高度复用重构后的 G6 图组件和左右侧边栏组件。
- **能力屏蔽**：隐藏所有右键菜单、新增/删除按钮、连线创建行为和右侧面板的编辑输入框。
- **保留能力**：保留画布拖拽缩放、节点拖拽、大纲树点击定位、查看节点属性详情。
- **全局开关**：在顶部操作栏实现“一键隐藏/显示所有实例”的 Switch 开关，调用 G6 的 `hideItem/showItem` 批量控制。

---

## ⚡ 给 AI 助手的执行指令 (Execution Guide)

请务必按照以下顺序逐步与我确认并修改代码，不要一次性输出全部文件的修改：

1. **阶段一 (Backend API & Logic)**：请先阅读 `extractor.py` 和相关路由，完成 `API 1`、`API 2` 的拆分，实现 `MD5` 确定性 ID 生成、术语规范化及 TTL 的反向序列化保存。
2. **阶段二 (Frontend G6 Swap)**：彻底卸载 React Flow。在前端引入 `@antv/g6`，完成圆形节点、虚线关系、力导向布局和坐标冻结的代码配置。
3. **阶段三 (UI/UX & State Machine)**：打通构建页的“上传 -> Schema 编辑 -> 实例提取”的状态流转。废弃 Drawer，实现左侧大纲树与右侧固定属性面板。
4. **阶段四 (Advanced Interactions)**：实现画布上的拖拽连线、Shift+Click 节点合并功能，以及全局的隐藏实例开关。

