# 🚀 OntologySystem 全栈架构重构与功能优化蓝图 (Vibe Coding Master Plan)

## 👤 角色与上下文设定 (Context & Persona)
你是一位精通 **React**、**AntV G6 (图引擎)**、**FastAPI**、**LLM 提示词工程**以及 **RDF/OWL 语义网标准**的资深全栈架构师。
当前项目是一个自动化的“知识图谱/本体构建系统”。现在的代码存在大模型提取混乱（类与实例不分）、前端连线杂乱、编辑交互被弹窗遮挡等痛点。
**你的任务**：完整重构该系统的核心链路，将其打造为一个“人机协同”、“支持意图定向”、“体验媲美 Neo4j”的专业图谱工程平台。

---

## 🛠️ 模块一：后端抽取引擎与数据治理 (Backend Architecture)
**核心思想：将黑盒的一步提取，拆分为严谨的“先搭骨架，后填血肉”两步走策略。**

### 1. 两阶段抽取 API (Two-Stage LLM Extraction)
*   **API 1：带意图的骨架提取 (Schema Extraction)**
    *   **输入**：文档文本 + `user_intent` (用户填写的意图字符串，可选)。
    *   **逻辑**：如果存在 `user_intent`，在 System Prompt 中强制模型聚焦该领域；否则进行通用核心概念提取。
    *   **约束**：**绝对禁止提取实例**。仅允许提取并返回 `OWL Class` (类)、`Object Property` (类与类的连线逻辑) 和 `Data Property` (属性字段)。
*   **API 2：强约束下的实例提取 (Instance Extraction)**
    *   **输入**：文档文本 + **完整 Schema Graph**（用户在前端审核并修改过后的类与关系 JSON 数据）。
    *   **逻辑**：将 Schema 转换为约束规则注入 Prompt。模型必须且只能实例化 Schema 中已定义的类，生成的连线必须符合 Schema 中的 Domain (起点) 和 Range (终点)。

### 2. 底层数据一致性治理
*   **确定性 ID (Deterministic ID)**：废弃 LLM 随机生成的英文 ID。后端接收数据时，必须使用 `MD5(实体Label + 节点类型)` 自动计算并覆盖系统 ID。确保同一概念在多次提取中绝对唯一。
*   **防御性校验拦截**：在 API 2 的解析环节，校验模型返回的连线，若不符合 Schema 约束，直接丢弃该“飞线”。
*   **全生命周期 TTL 同步**：在图谱“保存/发布”的接口中，增加反向序列化功能。使用 RDF 库 (如 `rdflib`) 将前端提交的最新 JSON 图数据 (Nodes & Edges) 重新解析并覆盖生成后台的 `.ttl` 物理文件。保证下载的 TTL 永远是最新快照。

---

## 🎨 模块二：前端图引擎重构 (AntV G6 Integration)
**核心思想：全面废弃 React Flow，引入 AntV G6 渲染引擎，复刻图数据库 (Neo4j) 的高级视觉体验。**

### 1. 视觉分层体系 (Visual Hierarchy)
*   **节点 (Nodes)**：
    *   **统一形状**：强制为完美正圆形 (`Circle`)，废弃任何矩形外框。文字居中，溢出显示省略号，悬浮显示全称。
    *   **类节点 (Class)**：大尺寸 (如 50-60px)，使用深色/冷色系背景（如蓝色），凸显图谱骨架。
    *   **实例节点 (Instance)**：小尺寸 (如 30-40px)，使用浅色/暖色系背景（如橙色）。
*   **连线 (Edges)**：
    *   **业务连线**（如“A包含B”）：实线 + 箭头 + 深色。
    *   **归属连线**（`rdf:type` / `instance_of`）：**必须保留箭头**（明确指向类），但样式配置为 **浅灰色 + 虚线 (`lineDash: [5, 5]`)**。以此弱化实例从属关系带来的视觉干扰。

### 2. 智能布局与防抖编辑 (Smart Layout & Stability)
*   **初始化布局**：加载数据时，启用 G6 的 `force` (力导向) 或 `radial` (环形辐射) 布局，让实例自动环绕父类节点自然舒展。
*   **坐标冻结 (关键)**：在布局算法完成 (监听 `afterlayout` 事件) 后，立刻锁定所有节点的 `x/y` 坐标。确保用户在手动拖拽节点、新增节点进行编辑时，图谱不会产生剧烈的全局物理重排抖动。

### 3. 画布内交互 (Canvas CRUD Behaviors)
*   启用 G6 的 `drag-canvas`, `zoom-canvas`, `drag-node`。
*   配置自定义交互 (如 `create-edge` Behavior)，支持用户使用鼠标拖拽建立两个节点间的连线。
*   支持选中节点/边后按下 Delete 键触发删除逻辑。

---

## 🧭 模块三：工作流与 UI/UX 重构 (Workflow & UI Navigation)
**核心思想：打造无遮挡、所见即所得的人机协同工作流。**

### 1. 四步状态机工作流 (State Machine)
*   **Step 1: 意图配置** -> 上传文件后，弹出 Modal 让用户输入“提取关注点”。点击确认调用 API 1。
*   **Step 2: 骨架审查 (Schema Mode)** -> G6 画布仅渲染蓝色类节点。用户拥有**全量编辑权**（右键/侧边栏新增类、连线）。
*   **Step 3: 提取实例 (Instance Mode)** -> 用户点击“提取实例”，携带当前画布状态调用 API 2。G6 增量渲染橙色实例节点。
*   **Step 4: 最终定稿** -> 微调数据，点击顶部【保存草稿】触发 TTL 同步。

### 2. 左侧：全局大纲树与极速定位 (Outline Tree)
*   提供折叠式目录树，结构为：`类 (Class) -> 其包含的实例 (Instances)`。
*   使用图标区分类型（如 🔵 类，🟠 实例）。
*   **核心交互**：点击树中的任意项，调用 G6 的 `graph.focusItem(id, true)` 视口 API，使画布平滑飞入并聚焦放大至该节点。

### 3. 右侧：沉浸式属性编辑面板 (Right Fixed Panel)
*   **彻底废弃任何遮挡画布的弹窗 (`Drawer` 或 `Modal`) 用于属性编辑**。
*   在屏幕最右侧固定一个宽度约 300px 的属性面板。
*   **双向数据流**：点击 G6 画布中的节点或边，右侧面板瞬间显示其详细信息。在右侧面板的原地输入框中修改 Label 或属性，失焦时直接调用 G6 的 `graph.updateItem` 实时刷新画布，并更新外部 React 状态以备保存。

---

## ⚡ 执行指令 (Execution Rules)
1. 请先仔细理解上述的业务流转逻辑，特别是 API 1 到 API 2 之间 Schema 数据的传递。
2. 开始重构前，请先设计好前后端交互的 JSON 数据契约 (Data Interface)。
3. 请逐步进行：先处理后端的双 API 拆分与 LLM Prompt 逻辑；再引入 AntV G6 替换前端渲染引擎并实现视觉配置；最后打通左右侧边栏的状态联动。